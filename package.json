{
  "scripts": {
    "build": "npm run build:public && npm run build:admin",
    "build:public": "node build.js public",
    "build:admin": "node build.js admin"
  },
  "devDependencies": {
    "terser": "^5.39.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.5",
    "jquery": "^3.7.1"
  }
}
